<Project>

  <PropertyGroup>
    <TargetFramework>netcoreapp1.0</TargetFramework>
    <RestoreSources>$(MSBuildThisFileDirectory)..\artifacts\</RestoreSources>
    <RestorePackagesPath>$(MSBuildThisFileDirectory)packages\</RestorePackagesPath>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MSBuildTasks.Sample" Version="0.1.0-*" PrivateAssets="All" />
  </ItemGroup>

  <Import Project="$(MSBuildToolsPath)\Microsoft.Common.targets" />

  <Target Name="Build" >
    <SayHello />
  </Target>

</Project>
